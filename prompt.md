Thanks that was great
Now focus on the plugins array and transcribe them into the corresponding css classes inside a component layer

Do not go for media queries instead just import the styles already present in the tailwind.config.js file